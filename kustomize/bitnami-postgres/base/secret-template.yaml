# This is a TEMPLATE file for documentation purposes
# DO NOT commit actual credentials to version control
#
# In production, use one of the following approaches:
# 1. Sealed Secrets (bitnami-labs/sealed-secrets)
# 2. External Secrets Operator (external-secrets/external-secrets)
# 3. Azure Key Vault CSI Driver
# 4. Manual creation via kubectl with proper RBAC
#
# To create the secret manually:
# kubectl create secret generic postgres-credentials \
#   --from-literal=postgres-password='<STRONG_PASSWORD>' \
#   --from-literal=password='<STRONG_PASSWORD>' \
#   --from-literal=replication-password='<STRONG_PASSWORD>' \
#   --namespace=postgres

apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: postgres
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/component: database
type: Opaque
stringData:
  postgres-password: "CHANGE_ME_STRONG_PASSWORD"
  password: "CHANGE_ME_STRONG_PASSWORD"
  replication-password: "CHANGE_ME_STRONG_PASSWORD"
