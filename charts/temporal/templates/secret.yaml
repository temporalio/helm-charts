{{- if .Values.additionalSecrets }}
{{- range .Values.additionalSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .name }}"
data:
{{- range $key, $val := .value }}
  {{ $key }}: {{ $val | b64enc | nindent 4 }}
{{- end }}
type: Opaque
{{- end }}
{{- end }}
